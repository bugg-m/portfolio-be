import { StringObjectTypes } from "@src/types/app.types";

const AdminRouteNames: StringObjectTypes = {
    LOGIN: "/login",
    LOGOUT: "/logout",
    REGISTER: "/register",
    GET_SUMMARY: "/summary",
    GITHUB_PROJECTS: "/githubProjects",
    PROJECTS: "/projects",
    REFRESH_ACCESS_TOKEN: "/refresh_access_token"
};

export { AdminRouteNames };
